<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sehyeon.aimstack" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/aim_stack"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar" >
        <activity android:name=".StartPage" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AimListPage" >
        </activity>
        <activity android:name=".AddNewAimPage" >
        </activity>
        <activity android:name=".StatisticsPage" >
        </activity>

        <activity android:name=".OngoingPage" >
        </activity>
    </application>

</manifest>
